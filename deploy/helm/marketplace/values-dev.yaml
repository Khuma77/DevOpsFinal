# Development environment values
replicaCount: 1

image:
  tag: "770d110853b22ad5aa8e741ec83664a121558782"

ingress:
  hosts:
    - host: marketplace-dev.company.com
      paths:
        - path: /
          pathType: Prefix

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: false

env:
  - name: ENVIRONMENT
    value: "development"
  - name: LOG_LEVEL
    value: "DEBUG"

# Disable persistence for dev environment
persistence:
  enabled: false