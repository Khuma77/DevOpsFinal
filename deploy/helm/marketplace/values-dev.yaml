# Development environment values
replicaCount: 1

image:
  tag: "11e481193786e4d113edcd60351c767e7b49b773"

ingress:
  hosts:
    - host: marketplace-dev.company.com
      paths:
        - path: /
          pathType: Prefix

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: false

env:
  - name: ENVIRONMENT
    value: "development"
  - name: LOG_LEVEL
    value: "DEBUG"

# Disable persistence for dev environment
persistence:
  enabled: false